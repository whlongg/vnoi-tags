# VNOI Tag Project

Static archive of the VNOI problem tag list.

```bash
python3 scripts/snapshot_public.py
npm test
python3 -m http.server 8000
```

Open <http://localhost:8000>. Edit `data.json` directly for future data changes.
